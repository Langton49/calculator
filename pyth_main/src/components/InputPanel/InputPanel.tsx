import React from 'react';

const InputPanel: React.FC = () => {
  return (
    <div className="input-panel">
      <h2>Input Panel</h2>
      {/* Input panel implementation will be added in subsequent tasks */}
    </div>
  );
};

export default InputPanel;