import React from 'react';

const Graph: React.FC = () => {
    return (
        <div className="graph">
            <h2>Graph</h2>
            {/* Graph implementation will be added in subsequent tasks */}
        </div>
    );
};

export default Graph;